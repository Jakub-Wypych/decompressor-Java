package decompressor.Dictionary;

public class Dictionary {
    /* Stores the Dictionary in a form of a tree
    can allow movement throughout the tree and can check if it reached the end (the "leaf")
     */
}
