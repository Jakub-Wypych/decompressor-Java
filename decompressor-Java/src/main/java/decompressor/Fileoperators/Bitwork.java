package decompressor.Fileoperators;

/* Used for reading/writing N bits */
public class Bitwork {
    /*boolean[] bits = new boolean[8];
    byte readbyte = compressedFile.ReadByte();
    for(int i=0; i<8;i++) {
            bits[i] = ((readbyte << i) & 0x80) != 0;
    }*/
}
